<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>csharp-test</AssemblyName>
  </PropertyGroup>

  <!-- Ссылка на сборку, сгенерированную компилятор-плагином (PE):
       Arithmetic.dll из Kotlin. Путь относительный к .csproj. -->
  <ItemGroup>
    <Reference Include="Arithmetic">
      <HintPath>..\..\..\build\00-int-add\Arithmetic.dll</HintPath>
    </Reference>
  </ItemGroup>

</Project>
