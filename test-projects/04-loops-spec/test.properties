# Test attributes; format and keys: docs/test-format.md
# Several sources (default *.kt): each program is built
# and checked separately (spec tests ported from kotlin/tests-spec).
kind=exe
expect=OK
desc=4 spec-теста while/do-while из kotlin/tests-spec (EXE, print OK)
