# see docs/test-format.md
# kind=negative: compilation must FAIL; expect = substring of the compiler error
kind=negative
expect=kotlin.text.isEmpty
desc=strict mode: uncovered kotlin.text.isEmpty fails compilation listing the symbol (no artifacts)
