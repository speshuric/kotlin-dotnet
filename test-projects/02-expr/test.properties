# Test attributes; format and keys: docs/test-format.md
# Without expect - only non-empty consumer output is checked
# (the output contains environment-dependent lines).
kind=dll
consumer=csharp-test
desc=16 выражений: арифметика, if/when, сравнения, Long/Double (DLL)
